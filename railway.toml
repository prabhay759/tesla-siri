[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "npm run start:siri"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
healthcheckPath = "/health"
healthcheckTimeout = 30
